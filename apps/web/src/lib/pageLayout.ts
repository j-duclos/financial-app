/** Full-width page shell with consistent horizontal gutters (matches Dashboard, Calendar, Transactions). */
export const PAGE_SHELL = "w-full px-4 sm:px-6 lg:px-8";

/** Default vertical padding for scrollable content pages. */
export const PAGE_SHELL_PY = `${PAGE_SHELL} py-4`;

/** Slightly roomier vertical padding for list-heavy pages. */
export const PAGE_SHELL_PY_LOOSE = `${PAGE_SHELL} py-6`;
